<h2>Welcome</h2>
<% if flash[:notice] %>
  <%= flash[:notice] %>
<% end %>

<%= link_to "About", about_path %>

<%= link_to "New gif", new_gif_path %>

<%= @gifs.to_a.each do |gif| %>
  <img src="<%= gif.url %>">
  <%= gif.description %>
<% end %>